<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nnew.cellmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".userpattern.UserPattern">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".selfcare.SelfCare" />
        <activity android:name=".setting.Setting" />
        <activity android:name=".setting.User" />
        <activity android:name=".setting.Notification" />
        <activity android:name=".selfcare.SelfCareBrain" />
        <activity android:name=".selfcare.SelfCareVisualization" />
        <activity android:name=".userpattern.UserPatternVisualization" />
        <activity android:name=".setting.Widget"></activity>
    </application>

</manifest>